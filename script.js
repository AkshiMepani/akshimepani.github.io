const grid=document.getElementById('project-grid');
projects.forEach(p=>{const card=document.createElement('article');card.className='project-card reveal';card.innerHTML=`<span class="tag">${p.category}</span><h3>${p.title}</h3><p>${p.description}</p><ul>${p.highlights.map(x=>`<li>${x}</li>`).join('')}</ul><p><strong>Tools:</strong> ${p.tools}</p><div><a href="${p.repo}" ${p.repo!=='#'?'target="_blank" rel="noopener"':''}>Repository ↗</a><a href="${p.demo}" ${p.demo!=='#'?'target="_blank" rel="noopener"':''}>View Project ↗</a></div>`;grid.appendChild(card)});
document.getElementById('year').textContent=new Date().getFullYear();
const menu=document.querySelector('.menu-toggle'),nav=document.querySelector('.nav-links');menu.addEventListener('click',()=>{nav.classList.toggle('open');menu.setAttribute('aria-expanded',nav.classList.contains('open'))});document.querySelectorAll('.nav-links a').forEach(a=>a.addEventListener('click',()=>nav.classList.remove('open')));
const observer=new IntersectionObserver(entries=>entries.forEach(e=>{if(e.isIntersecting)e.target.classList.add('visible')}),{threshold:.12});document.querySelectorAll('.reveal').forEach(el=>observer.observe(el));
